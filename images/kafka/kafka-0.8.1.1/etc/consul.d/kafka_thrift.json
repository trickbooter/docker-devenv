{
    "service": {
        "name": "kafka",
        "port": 9092,
        "tags" : ["v0.8.1.1", "Thrift"],
        "check": {
            "name": "Kafka JMX Check",
            "notes": "Connect to the port internally every 10 seconds",
            "script": "nc -z -v localhost 9092 >/dev/null 2>&1",
            "interval": "10s"
        }
    }
}
